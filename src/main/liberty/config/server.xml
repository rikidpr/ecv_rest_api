<?xml version="1.0" encoding="UTF-8"?>
<server description="${project.name}">

	<featureManager>
		<feature>microProfile-3.0</feature>
	</featureManager>

	<httpEndpoint id="defaultHttpEndpoint" httpPort="8181"
		httpsPort="8543" />

	<webApplication location="${project.name}.war"
		contextRoot="/" />
	<mpMetrics authentication="false" />

	<!-- This is the keystore that will be used by SSL and by JWT. -->
	<keyStore id="defaultKeyStore" location="public.jks"
		type="JKS" password="atbash" />


	<!-- The MP JWT configuration that injects the caller's JWT into a ResourceScoped 
		bean for inspection. -->
	<mpJwt id="jwtUserConsumer" keyName="theKeyId"
		audiences="targetService" issuer="${jwt.issuer}" />

	<variable name="DERBY_JDBC_DRIVER_PATH" value="/home/riki/workspace/ecv/derby-10.15.1.3.jar"/>
    <library id="DerbyLib">
        <file name="${DERBY_JDBC_DRIVER_PATH}"/>
    </library>
	

	<dataSource id="ecvDS" jndiName="jdbc/ecvDS">
		<jdbcDriver libraryRef="DerbyLib" />
		<!-- Example properties referencing an in-memory Derby Embedded database -->
		<properties.derby.embedded
			databaseName="memory:defaultdb" createDatabase="create" user="dbuser"
			password="dbpass" />
	</dataSource>
</server>
